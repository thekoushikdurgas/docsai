# Smoke verification (first 5 CSV rows)

Generated by:

`python ko.py --input "input/instantlead.net Client A0560 - Sheet1.csv" --output-dir output --limit 5`

## Summary (`output/conversion_summary.json`)

| Metric | Value |
|--------|-------|
| rows_total | 5 |
| rows_with_url | 5 |
| rows_with_unmapped | 5 |
| rows_with_warnings | 3 |
| rows_parse_issue | 0 |

## Per-row notes

1. **Row 0** (`f5e3ba93-…`): Titles, US location, Outlook technographic, verified email, revenue min, sort. `prospectedByCurrentTeam` → **unmapped** (expected). Revenue → `company_annual_revenue.gte`.

2. **Row 1** (`6aacc7eb-…`): Many org employee ranges (9 buckets) → **unmapped** + warning `multiple_organizationNumEmployeesRanges_*` because VQL cannot OR multiple `range_query` filters. Org keyword include/exclude lists map to `company_name` text matches.

3. **Row 2** (`e0bc2461-…`): `qOrganizationSearchListId` → **unmapped**. Titles, not-titles, seniorities, China exclusions applied.

4. **Row 3** (`79d871cd-…`): Two employee ranges → **unmapped** + same warning. Dual email status → `email_status` as multi-value for `terms` semantics.

5. **Row 4** (`5725ff35-…`): Three employee ranges + org search list id → **unmapped** + warning.

## Edge cases observed in the full CSV (manual)

- Truncated URLs may end with `qOrganizationKeyw` — parser sets `parse_issues` when detected.
- Odd parameter stems (`D`, ` page`, `qOrganizationKeywordTags[` with space) appear once — surfaced under **`unmapped`** for unknown keys.

## Manual VQL sanity check

Compare any `vql` object in `vql_export.jsonl` with the schema in `EC2/sync.server/README.md` (VQL section) and `utilities/structures.go`: `where.text_matches`, `where.keyword_match`, `where.range_query`, optional `order_by`, `page`, `limit`.
