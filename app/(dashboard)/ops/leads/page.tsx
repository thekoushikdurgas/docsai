import { AdminPageLayout } from "@/components/layouts/AdminPageLayout";

export default function OpsLeadsPage() {
  return (
    <AdminPageLayout
      title="Leads"
      subtitle="Placeholder — not implemented in Django (admin_ops:ops_leads)"
    >
      <p className="c360-mm-lead">No leads explorer is wired yet.</p>
    </AdminPageLayout>
  );
}
