{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contact360.io/docs/schemas/hub",
  "title": "Hub / Policy Document",
  "description": "Canonical policy and architecture markdown under docs/docs/*.md (folder_kind_map rule hub_docs). Full markdown in raw_markdown; sections[] is structured projection.",
  "allOf": [{ "$ref": "common_envelope.schema.json" }],
  "properties": {
    "kind": { "const": "hub" },
    "sections": {
      "type": "array",
      "description": "Document sections in order with derived tables, lists, code fences, links",
      "items": { "$ref": "shared_defs.schema.json#/$defs/enriched_section" }
    }
  }
}
