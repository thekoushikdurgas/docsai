Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-q2==1.9.0
django-picklefield==3.4.0
django-storages==1.14.2  # S3 storage backend for Django
whitenoise==6.6.0  # Static file serving (fallback if S3 not configured)
gunicorn==21.2.0  # WSGI server for production (EC2, Docker)
python-dotenv==1.0.0
boto3==1.34.0
httpx==0.27.2
google-generativeai==0.3.2
openai==1.12.0
Pillow==10.2.0
psycopg2-binary==2.9.9
redis==5.0.1  # Optional: Only needed if USE_REDIS_CACHE=True (default uses LocMemCache)
Markdown==3.5.1
pydantic==2.5.0  # For request validation schemas
drf-spectacular==0.27.0  # OpenAPI 3.0 schema generation for Django REST Framework
coverage==7.3.4  # For test coverage reporting
responses==0.24.1  # For mocking HTTP requests in tests
moto==5.0.0  # For mocking AWS services (S3) in tests
pytest==7.4.3  # For running tests
pytest-django==4.7.0  # Django integration for pytest
pytest-playwright==0.4.3  # Playwright integration for pytest
playwright==1.40.0  # Browser automation for E2E tests
ruff==0.1.15  # Fast Python linter (replaces Flake8, Pylint, isort)
black==23.12.1  # Code formatter
isort==5.13.2  # Import sorter
mypy==1.8.0  # Static type checker
pre-commit==3.6.0  # Git hooks framework (optional)
