# Django admin was removed from the project (django.contrib.admin not in INSTALLED_APPS).
# Models: Workflow, WorkflowNode, WorkflowConnection, Execution, ExecutionLog, Credential, WorkflowTemplate.
# Re-add admin registrations here if Django admin is re-enabled.
