# ============================================================================
# Django Docs AI Agent - Environment Variables Configuration
# ============================================================================
# 
# Copy this file to .env and fill in your actual values
#   cp env.example .env
#
# Run validation after setup:
#   python manage.py validate_env
#
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: Django Settings
# ----------------------------------------------------------------------------
# Generate a secret key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=y%q9x&9q%(m0-1oz59$sml5zvvlga%be_2p=ri^$br-1qt+io%
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Environment selection (development/staging/production/testing)
DJANGO_ENV=development

# ----------------------------------------------------------------------------
# Production / SSL (for EC2 deploy)
# ----------------------------------------------------------------------------
# For HTTP-only deploy (e.g. http://34.201.10.84), set to false so the UI works over HTTP.
# For deploy with domain + SSL (https://), set to true.
# SECURE_SSL_REDIRECT=false

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
# For development: Use SQLite (default, no configuration needed)
# For production: Set DATABASE_ENGINE=postgresql and configure below

# Database engine: 'sqlite' (default) or 'postgresql'
DATABASE_ENGINE=sqlite
# ----------------------------------------------------------------------------
# RECOMMENDED: AWS S3 Configuration (for file storage)
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# RECOMMENDED: Lambda API Endpoints
# ----------------------------------------------------------------------------
# Required for external documentation and AI API integration
# LAMBDA_DOCUMENTATION_API_URL=https://94hucnirj2.execute-api.us-east-1.amazonaws.com
# LAMBDA_DOCUMENTATION_API_URL=http://127.0.0.1:8080
# LAMBDA_DOCUMENTATION_API_KEY=bc7a0177de676a8e8bd98c2a0e6f96152b7b1ae1e72eb3d108ed13d5f01fd9bd
LAMBDA_AI_API_URL=https://aziwa531nl.execute-api.us-east-1.amazonaws.com
LAMBDA_AI_API_KEY=bc7a0177de676a8e8bd98c2a0e6f96152b7b1ae1e72eb3d108ed13d5f01fd9bd

# ----------------------------------------------------------------------------
# OPTIONAL: OpenAI Configuration
# ----------------------------------------------------------------------------
# Required for AI chat features (falls back to Lambda AI API if not set)
# Remove or comment out if not using OpenAI (Lambda AI will be used instead)
# OPENAI_API_KEY=your-openai-api-key

# ----------------------------------------------------------------------------
# OPTIONAL: Redis Configuration (DISABLED - using Local Memory Cache)
# ----------------------------------------------------------------------------
# Cache uses LocMemCache. Set USE_REDIS_CACHE=True and Redis vars to enable Redis.
# USE_REDIS_CACHE=False
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_URL=redis://localhost:6379/0

# ----------------------------------------------------------------------------
# OPTIONAL: Django-Q Configuration
# ----------------------------------------------------------------------------
# Django-Q worker and task queue settings (defaults shown)
DJANGO_Q_WORKERS=4
DJANGO_Q_RECYCLE=500
DJANGO_Q_TIMEOUT=300
DJANGO_Q_RETRY=300
DJANGO_Q_QUEUE_LIMIT=50
DJANGO_Q_BULK=10
DJANGO_Q_SAVE_LIMIT=250
DJANGO_Q_ERROR_LIMIT=250

# ----------------------------------------------------------------------------
# OPTIONAL: Google Gemini Configuration
# ----------------------------------------------------------------------------
# GEMINI_API_KEY=your-gemini-api-key

# ----------------------------------------------------------------------------
# OPTIONAL: Email Configuration
# ----------------------------------------------------------------------------
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password

# ----------------------------------------------------------------------------
# OPTIONAL: Logging Configuration
# ----------------------------------------------------------------------------
# LOG_LEVEL=INFO

# ----------------------------------------------------------------------------
# OPTIONAL: Application Settings
# ----------------------------------------------------------------------------
TIME_ZONE=UTC
LANGUAGE_CODE=en-us
USE_LOCAL_JSON_FILES=True

# ----------------------------------------------------------------------------
# OPTIONAL: Appointment360 GraphQL Configuration
# ----------------------------------------------------------------------------
APPOINTMENT360_GRAPHQL_URL=http://100.53.186.109/graphql
# Note: APPOINTMENT360_GRAPHQL_API_KEY removed - authentication now uses JWT tokens from login sessions
# Set GRAPHQL_AUTH_ENABLED=False if GraphQL API doesn't require authentication
GRAPHQL_AUTH_ENABLED=True
S3_AUTH_STORAGE_ENABLED=True
