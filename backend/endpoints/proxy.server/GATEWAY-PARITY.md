# proxy.server — gateway parity (planned)

- **Today:** Call **`proxy.server` REST** directly from `email.server` or ops tools using `X-API-Key`.
- **Next:** Add **`ProxyServerClient`** in `contact360.io/api` and a **`health.satelliteHealth`** row **`proxy`** once product GraphQL needs it.
