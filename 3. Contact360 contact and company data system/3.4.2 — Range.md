# 3.4.2 — Range

- **Era:** `3.x` Contact/company data — hub [`versions.md`](../versions.md) · minors start at [`3.0 — Twin Ledger`](3.0%20%E2%80%94%20Twin%20Ledger.md)
- **Minor:** [3.4 — Dashboard UX](./3.4 — Dashboard UX.md)
- **Codename:** Range
- **Status:** planned

## Focus
Range controls

## Flowchart

```mermaid
flowchart LR
    patchNode["3.4.2 — Range"] --> workNode["Range controls"]
    workNode --> gateNode["Micro-gate closeout"]
```

## Micro-gate

| Track | Gate question | Answer / Evidence (fill at patch closeout) |
| --- | --- | --- |
| **Contract** | GraphQL, Connectra REST, or VQL contract changed? Diff vs `docs/backend/apis/` + endpoint matrices. | Document at patch closeout. |
| **Service** | List/count/batch-upsert, gateway clients, processors — smoke + idempotency story intact? | Document smoke paths. |
| **Surface** | Dashboard contacts/companies or admin paths changed? Filters, exports, error UX? | Document UX delta or N/A. |
| **Frontend** | Which routes/hooks/components for this patch? | Saved search, export, drill-down modals. Document at closeout. |
| **Data** | PG+ES lineage, enrichment/dedup, job artifacts — migrations + docs? | Document lineage or N/A. |
| **Ops** | Queues, drift jobs, logs PII rules, runbooks — delta recorded? | Document ops delta or N/A. |

## Tasks
### Contract

- 📌 Planned: Saved search payload ↔ **stored VQL** or equivalent JSON.  
- 📌 Planned: Export job create carries **query fingerprint** for lineage.

### Service

- 📌 Planned: Server validates saved search size and filter depth (abuse guard).

### Surface

- 📌 Planned: Routes: [`dashboard-search-ux.md`](dashboard-search-ux.md).  
- 📌 Planned: Feature access: `ADVANCED_FILTERS`, `SAVE_SEARCHES`.

### Data

- 📌 Planned: **User-owned** saved search rows; GDPR delete path.

### Ops

- 📌 Planned: Funnel metric: **search-to-export conversion** (roadmap KPI).

## Service task slices
> Merged from era `3.x` contact/company task packs (P0→`.0`–`.2`, P1→`.3`–`.6`, Ops→`.7`–`.9`).

### Appointment360 (gateway)
- Define ContactQuery { contact(uuid), contacts(query), contactCount(query), contactQuery(query), filters(), filterData(input) }
- Define ContactMutation { createContact, batchCreateContacts, updateContact, deleteContact, exportContacts, batchUpsertContacts, upsertByLinkedinUrl, searchLinkedin, syncIntegration }
- Define CompanyQuery { company(uuid), companies(query), companyCount(query), companyQuery(query), companyContacts(company_uuid), filters(), filterData(input) }
- Define CompanyMutation { createCompany, batchCreateCompanies, updateCompany, deleteCompany, exportCompanies }
- Define VQLQueryInput type: where, sort, page, per_page fields
- Define ContactType, CompanyType, FilterType, FilterDataType GraphQL output types
- Implement ConnectraClient in app/clients/connectra_client.py
- Wire contacts(query) → ConnectraClient.query_contacts(vql_input)
- Wire contactCount(query) → ConnectraClient.count_contacts(vql_input)
- Wire contact(uuid) → ConnectraClient.query_contacts with uuid filter
- Wire contacts.filters() → ConnectraClient.get_filters()
- Wire contacts.filterData(input) → ConnectraClient.get_filter_data(input)
- Wire batchUpsertContacts → ConnectraClient.batch_upsert_contacts(...)
- Wire exportContacts → TkdjobClient.create_contact360_export(...)
- Wire companies(query) → ConnectraClient.query_companies(vql_input)
- Wire companyContacts(company_uuid) → ConnectraClient.query_contacts with company filter
- Implement vql_converter.py: translate GraphQL VQLQueryInput → Connectra REST format
- Implement connectra_mappers.py: map Connectra JSON responses → GraphQL types
- /contacts page, list tab → query contacts(query: VQLQueryInput) binding
- Contacts filter sidebar → query filters() + query filterData(input) binding
- Contacts search bar → VQL where input builder
- Contacts export button → mutation exportContacts → job polling
- Contact detail modal → query contact(uuid) binding
- Import contacts modal (CSV) → mutation createContact360Import via jobs module
- /companies page grid/list → query companies(query) binding
- Company detail view, contacts tab → query companyContacts(company_uuid)
- Pagination controls → per_page/page in VQLQueryInput
- Table sort headers → sort field in VQLQueryInput
- useContactsPage hook: state for query, pagination, sort, loading
- useContactsFilters hook: load filters, apply filter selections
- useCompaniesPage hook: mirror of contacts page hook
- contactsService.ts: listContacts, getContact, contactCount, getContactFilters
- companiesService.ts: listCompanies, getCompany, companyContacts
- Contacts and companies data stored in Connectra (PostgreSQL + Elasticsearch) — not appointment360 DB
- Track exportContacts job uuid in activities table (appointment360 DB)
- Track batchUpsertContacts usage for credit deduction
- Configure CONNECTRA_BASE_URL, CONNECTRA_API_KEY, CONNECTRA_TIMEOUT in .env.example
- Test query contacts round-trip with Connectra dev instance

## Evidence gate
Patch closeout includes contract diff, smoke output, data lineage delta, and ops note
