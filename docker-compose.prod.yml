# Production stack for EC2: Next.js standalone image (see Dockerfile).
# Build-time/public env vars come from .env.production.
services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      PORT: "3000"
    restart: unless-stopped
